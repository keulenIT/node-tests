// functions to be tested
// can also be written on one line because it's a single return statement
function add(a, b) {
  return a + b;
}

function square(x) {
  x * x;
}

module.exports = {
  add,
  square,
};
